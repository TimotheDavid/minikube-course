apiVersion: v1
kind: Service
metadata: 
  name: "{{  .Values.nameService }}"
  namespace: {{ .Values.namespaces.name }}
spec:
  selector:
    app: "{{ .Values.name }}"
  type: LoadBalancer
  ports:
    - port: 4242
      targetPort: 4242
      
      